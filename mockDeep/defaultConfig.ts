import Config from './Config'

const defaultConfig: Config = {
  mockParam: 'mock',
  mockParamThisOrDeep: 'thisOrDeep',
  mockParamDeep: 'deep',
  mocksDir: '__mocks__'
}

export default defaultConfig
